# Core
anthropic>=0.40.0
python-dotenv>=1.0.0

# Scheduling
apscheduler>=3.10.0

# HTTP / scraping
httpx>=0.27.0
playwright>=1.44.0
playwright-stealth>=1.0.6

# Database
# (sqlite3 is stdlib — no install needed)

# Email
resend>=2.0.0

# Payments
stripe>=9.0.0

# Webhook server
flask>=3.0.0

# PDF generation (lead package)
reportlab>=4.2.0

# Utilities
tenacity>=8.3.0   # retry with backoff
fake-useragent>=1.5.0
